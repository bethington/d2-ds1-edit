name: Build & Test

on:
  push:
    branches: [main, fix-interactive-rendering]
  pull_request:
    branches: [main]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      # Dependencies come from vcpkg.json (manifest mode); the CMake
      # toolchain installs them during configure. An explicit
      # `vcpkg install <pkg>` is rejected in manifest mode.
      # GitHub runners expose vcpkg as VCPKG_INSTALLATION_ROOT; the ci preset
      # reads VCPKG_ROOT, so bridge the two before configuring.
      - name: Point VCPKG_ROOT at the runner's vcpkg
        shell: pwsh
        run: echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" >> $env:GITHUB_ENV

      - name: Configure
        run: cmake --preset ci

      - name: Build Release
        run: cmake --build --preset ci-release

      - name: Run tests
        run: ctest --preset ci

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ds1edit-windows
          path: |
            bin/ds1edit.exe
            data/
            pcx/
            assets/*.ini
            Ds1edit.ini.sample
          retention-days: 30
