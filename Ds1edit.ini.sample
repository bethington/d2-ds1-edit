; DS1Edit Configuration File
; ==========================
; Copy this file to "Ds1edit.ini" and edit the paths below to match
; your Diablo II installation directory.

; D2 install directory. When set, the editor resolves the standard Blizzard
; MPQs (patch_d2, d2exp, d2data, d2char) from this folder automatically.
; If left blank, the editor auto-detects via the Windows registry and a short
; list of common install paths.
; =================================================================
d2_install =

; You can still override individual MPQs below. An explicit path here always
; wins over d2_install / auto-detection. Leave blank to let resolution fill
; the slot from d2_install. When a file is loaded, the editor scans mod_dir
; first, then patch_d2, d2exp, d2data, d2char (in that order, matching D2).
; =================================================================
d2char   =
d2data   =
d2exp    =
patch_d2 =
mod_dir  =

; Optional remote PNG upscaling service. When enabled and a base URL is set,
; export actions can offer None / 2x / 4x modes using the configured service.
; ===========================================================================
upscale_enabled     = NO
upscale_service_url =

; PNG export filter. When YES (default), multi-frame DC6 files (animations,
; multi-direction sprites) are skipped during export. Multi-frame export to
; a dedicated animation format is a planned future feature; for now PNG
; output is single-frame only. Flip to NO to fall back to the prior behavior
; (export every frame of multi-frame DC6 alongside single-frame icons).
; ==========================================================================
export_dc6_single_frame_only = YES

; Examples of explicit configuration:
; d2_install = C:\Diablo2
; d2exp      = D:\custom\d2exp.mpq
; mod_dir    = C:\Diablo2\mods\my_mod
; upscale_enabled     = YES
; upscale_service_url = http://10.0.10.30:8084

; data_dir overrides where the editor looks for its runtime data
; (palettes, gamma table, obj.txt, ds1edit.dt1, undo temp files).
; Default is "Data\" relative to the exe. Leave blank for default.
; data_dir =


; screen configuration
; if full_screen is not set to YES, it'll be a windowed screen
; =============================================================
full_screen   = NO
screen_width  = 1280
screen_height = 720


; Try to use this refresh rate, if possible. Not all drivers are able to
; control this at all, and even when they can, not all rates will be
; possible on all hardware, so the actual settings may differ from what
; you requested. Some non-exhaustive values : 60, 70, 72, 75, 85, 100, 120
; ========================================================================
refresh_rate = 60


; speed of scrolls, in pixels
; ===========================
keyb_scroll_x  = 40
keyb_scroll_y  = 20
mouse_scroll_x = 20
mouse_scroll_y = 10
edit_scroll_x  = 20
edit_scroll_y  = 20


; speed of scroll in the Object Editing window, in rows
; =====================================================
obj_edit_scroll = 2


; misc options
; ============


; default gamma correction
; valid value : 0.60, 0.62, [...] 0.98, 1.00, 1.10, 1.20, [...], 3.00
; -------------------------------------------------------------------
gamma_correction = 1.20


; does the editor allow you to use Type 2 objects of higher act ?
; if this line is NOT set to YES, the editor will allow it
; ---------------------------------------------------------------
only_normal_type2 = YES


; does the editor always modified the ds1 to have 2 floors layers and
; 4 walls layers ?
; if this line is NOT set to YES, the editor won't make any change
; -------------------------------------------------------------------
always_max_layers = YES


; resize the sprites when zooming
; -------------------------------
stretch_sprite = YES


; when editing an object, posibility to scroll the main tile editing window
; with the arrow keys
; -------------------------------------------------------------------------
winobj_can_scroll_keyb = YES


; when editing an object, posibility to scroll the main tile editing window
; with the mouse on the border of the screen
; -------------------------------------------------------------------------
winobj_can_scroll_mouse = NO


; after a Centering (with the 'C' key), wich zoom ?
; possible values : NO_CHANGE, 1:1, 1:2, 1:4, 1:8, 1:16
; -----------------------------------------------------
center_zoom = NO_CHANGE


; default zoom level when opening a DS1 file
; possible values : 1:1, 1:2, 1:4, 1:8, 1:16
; -----------------------------------------------
default_zoom = 1:1


; What are the sizes of object tables in DLL for type 1 and 2 ?
; normal values are :
;    *  60 entries per act for type 1 objects
;    * 150 entries per act for type 2 objects
; You shouldn't change these values unless you have a modified DLL
; ----------------------------------------------------------------
nb_type1_per_act = 60
nb_type2_per_act = 150


; minimize file size of saved ds1 ?
; ---------------------------------
ds1_saved_minimize = YES


; reduce scrolling speed when zooming out ?
; -----------------------------------------
lower_speed_zoom_out = NO


; enable the workspace feature ?
; ------------------------------
workspace_enable = YES


; When YES, the compose-mode export action labels its output folders
; with full descriptive names (e.g. "Necromancer", "Andariel") drawn
; from the appropriate TXT tables. Filenames keep their compact 2-char
; D2 codes regardless. When NO (default), folders also use the
; 2-char codes so the output mirrors the MPQ structure exactly.
; ============================================================================
compose_use_full_folder_names = NO


; Export presets shown in the unified Ctrl+Shift+A export action's scope
; picker. Each entry is "name = type | pattern" where type is one of
; all/dt1/dc6/dcc and pattern is a glob matched against virtual asset
; paths under the mod overlay. Order in this section is the order in the
; picker. Wildcard syntax: * (any chars except \), ? (one char except \),
; ** (any number of subfolders, including zero).
; ===========================================================================
[export_presets]
items_all     = dc6 | data\global\items\*.dc6
items_inv     = dc6 | data\global\items\inv*.dc6
items_potions = dc6 | data\global\items\pot*.dc6
tiles_all     = dt1 | data\global\tiles\**\*.dt1
tiles_act1    = dt1 | data\global\tiles\ACT1\**\*.dt1
tiles_act2    = dt1 | data\global\tiles\ACT2\**\*.dt1
tiles_act3    = dt1 | data\global\tiles\ACT3\**\*.dt1
tiles_act4    = dt1 | data\global\tiles\ACT4\**\*.dt1
tiles_act5    = dt1 | data\global\tiles\ACT5\**\*.dt1


; Animation mode presets for the compose-mode multi-select picker.
; Each entry is "name = comma-separated D2 mode codes". Mode codes
; commonly used: NU (idle), TW (town walk), WL (walk), RN (run),
; A1 (attack 1), A2 (attack 2), DT (death), GH (get hit), SC (cast),
; TH (throw), KK (kick), BL (block), S1..S8 (skill 1..8). The picker
; also always shows an "All modes" entry as a fallback.
; ===========================================================================
[char_mode_presets]
idle_only       = NU
idle_walk       = NU, WL
standard_combat = NU, WL, A1, DT


; Weapon-class presets for the compose-mode multi-select picker
; (only shown for player chars; monsters/NPCs/objects don't have
; weapon variants). Codes: HTH (bare hands), 1HS (1H sword),
; 1HT (1H throwing), 2HS (2H sword), 2HT (2H throwing), BOW, XBW,
; STF (staff), HT1, HT2 (twin handed). The picker also always
; shows an "All weapons" entry as a fallback.
; ===========================================================================
[char_weapon_presets]
bare_hands = HTH
melee      = HTH, 1HS, 2HS
ranged     = HTH, BOW, XBW
magic      = HTH, STF
common     = HTH, 1HS, BOW, STF
